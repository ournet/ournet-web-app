import { IOurnetAppData } from "../ournet/data";

export const PlaceNoAdmin1Fields = 'id name names longitude latitude timezone countryCode';

export interface WeatherAppData extends IOurnetAppData {
    
}
